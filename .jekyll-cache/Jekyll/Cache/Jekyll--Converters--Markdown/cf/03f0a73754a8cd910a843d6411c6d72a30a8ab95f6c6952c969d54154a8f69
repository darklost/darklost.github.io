I"�<h3 id="问题描述">问题描述</h3>
<p>Mac不能解压rar文件，但实际中会需要这项功能。比如从邮件中的附件时常以rar文件格式传输。</p>

<h3 id="步骤">步骤</h3>

<ol>
  <li>进入<a href="http://www.rarlab.com/download.htm">rarlab</a>下载<code class="highlighter-rouge">RAR 5.20 beta 1 for Mac OS X</code></li>
  <li>打开Mac终端，执行解压缩命令：tar xvfz rarosx-5.2.b1.tar进行解压</li>
  <li>进入刚刚解压的rar目录中，cd downloads/rar</li>
  <li>在rar目录下使用如下命令进行安装</li>
</ol>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sudo install -c -o$USER unrar /bin
sudo install -c -o$USER rar /bin
</code></pre></div></div>

<h3 id="解压与压缩">解压与压缩</h3>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>unrar x filename.rar             #解压
rar a archivename.rar filename   #压缩
</code></pre></div></div>

<hr />

:ET